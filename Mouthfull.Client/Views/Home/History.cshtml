@model List<RecipeSummary>



<row class="card-group">

    @foreach(var recipe in Model)
    {
        <div class="col-sm">
            <div class="card">
                <div class="card-body">
                    <a href=@Url.Action("RecipeSummary","Home", new { id = recipe.id }) class="card-title">@recipe.title</a>
                </div>
            </div>
        </div>
    }
</row>